mybatis-plus:
  mapper-locations: classpath*:/mapper/**Mapper.xml

spring:
  redis:
    host: localhost
    port: 6379
  data:
    elasticsearch:
      uris: http://localhost:9200
      connection-timeout: 5s
      socket-timeout: 60s